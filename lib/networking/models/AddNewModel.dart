 import 'package:helpy/views/AddNewNeeded.dart';

class AddNewModel {
  var title;
  var gender;
  var mental_state;
 var adult;
  var latitude;
 var longitude;
 var proof;
 var description ;

  AddNewModel(this.title, this.gender, this.mental_state, this.adult,
      this.latitude, this.longitude, this.proof, this.description);
}